/*
 * API PIX
 * <pre>[ Base URL: https://api.sicoob.com.br/pix/api/v2 ]</pre> A API Pix Sicoob é uma forma fácil e segura dos associados gerenciarem os seus recebimentos Pix. Ela permite criar cobranças e validar a situação do pagamento, bem como verificar os Pix recebidos.  Seguindo os padrões definidos pelo Bacen, esta API utiliza as melhores práticas de segurança e possui autenticação com TLS mútuo, como uma medida adicional de proteção.
 *
 * The version of the OpenAPI document: 2.0.23.28
 * Contact: suporte.pix@bcb.gov.br
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package br.com.sicoob.pix.model;

import java.util.Objects;
import br.com.sicoob.pix.model.CobVGerada;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.time.OffsetDateTime;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import br.com.sicoob.pix.JSON;

/**
 * LoteCobVGerado
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2026-05-04T12:32:37.814990-03:00[America/Sao_Paulo]", comments = "Generator version: 7.22.0")
public class LoteCobVGerado {
  public static final String SERIALIZED_NAME_ID = "id";
  @SerializedName(SERIALIZED_NAME_ID)
  @javax.annotation.Nonnull
  private Long id;

  public static final String SERIALIZED_NAME_DESCRICAO = "descricao";
  @SerializedName(SERIALIZED_NAME_DESCRICAO)
  @javax.annotation.Nonnull
  private String descricao;

  public static final String SERIALIZED_NAME_CRIACAO = "criacao";
  @SerializedName(SERIALIZED_NAME_CRIACAO)
  @javax.annotation.Nonnull
  private OffsetDateTime criacao;

  public static final String SERIALIZED_NAME_COBSV = "cobsv";
  @SerializedName(SERIALIZED_NAME_COBSV)
  @javax.annotation.Nonnull
  private List<CobVGerada> cobsv = new ArrayList<>();

  public LoteCobVGerado() {
  }

  public LoteCobVGerado id(@javax.annotation.Nonnull Long id) {
    this.id = id;
    return this;
  }

  /**
   * Get id
   * @return id
   */
  @javax.annotation.Nonnull
  public Long getId() {
    return id;
  }

  public void setId(@javax.annotation.Nonnull Long id) {
    this.id = id;
  }


  public LoteCobVGerado descricao(@javax.annotation.Nonnull String descricao) {
    this.descricao = descricao;
    return this;
  }

  /**
   * Get descricao
   * @return descricao
   */
  @javax.annotation.Nonnull
  public String getDescricao() {
    return descricao;
  }

  public void setDescricao(@javax.annotation.Nonnull String descricao) {
    this.descricao = descricao;
  }


  public LoteCobVGerado criacao(@javax.annotation.Nonnull OffsetDateTime criacao) {
    this.criacao = criacao;
    return this;
  }

  /**
   * Timestamp que indica o momento em que foi criado o lote. Respeita o formato definido na RFC 3339.
   * @return criacao
   */
  @javax.annotation.Nonnull
  public OffsetDateTime getCriacao() {
    return criacao;
  }

  public void setCriacao(@javax.annotation.Nonnull OffsetDateTime criacao) {
    this.criacao = criacao;
  }


  public LoteCobVGerado cobsv(@javax.annotation.Nonnull List<CobVGerada> cobsv) {
    this.cobsv = cobsv;
    return this;
  }

  public LoteCobVGerado addCobsvItem(CobVGerada cobsvItem) {
    if (this.cobsv == null) {
      this.cobsv = new ArrayList<>();
    }
    this.cobsv.add(cobsvItem);
    return this;
  }

  /**
   * Get cobsv
   * @return cobsv
   */
  @javax.annotation.Nonnull
  public List<CobVGerada> getCobsv() {
    return cobsv;
  }

  public void setCobsv(@javax.annotation.Nonnull List<CobVGerada> cobsv) {
    this.cobsv = cobsv;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    LoteCobVGerado loteCobVGerado = (LoteCobVGerado) o;
    return Objects.equals(this.id, loteCobVGerado.id) &&
        Objects.equals(this.descricao, loteCobVGerado.descricao) &&
        Objects.equals(this.criacao, loteCobVGerado.criacao) &&
        Objects.equals(this.cobsv, loteCobVGerado.cobsv);
  }

  @Override
  public int hashCode() {
    return Objects.hash(id, descricao, criacao, cobsv);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class LoteCobVGerado {\n");
    sb.append("    id: ").append(toIndentedString(id)).append("\n");
    sb.append("    descricao: ").append(toIndentedString(descricao)).append("\n");
    sb.append("    criacao: ").append(toIndentedString(criacao)).append("\n");
    sb.append("    cobsv: ").append(toIndentedString(cobsv)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    return o == null ? "null" : o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("id", "descricao", "criacao", "cobsv"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(Arrays.asList("id", "descricao", "criacao", "cobsv"));
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to LoteCobVGerado
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!LoteCobVGerado.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in LoteCobVGerado is not found in the empty JSON string", LoteCobVGerado.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!LoteCobVGerado.openapiFields.contains(entry.getKey())) {
          // throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` in the JSON string is not defined in the `LoteCobVGerado` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : LoteCobVGerado.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (!jsonObj.get("descricao").isJsonPrimitive()) {
        // throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `descricao` to be a primitive type in the JSON string but got `%s`", jsonObj.get("descricao").toString()));
      }
      if (jsonObj.get("cobsv") != null) {
        if (!jsonObj.get("cobsv").isJsonArray()) {
          // throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `cobsv` to be an array in the JSON string but got `%s`", jsonObj.get("cobsv").toString()));
        }
        JsonArray jsonArraycobsv = jsonObj.getAsJsonArray("cobsv");
        // validate the required field `cobsv` (array)
        for (int i = 0; i < jsonArraycobsv.size(); i++) {
          CobVGerada.validateJsonElement(jsonArraycobsv.get(i));
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!LoteCobVGerado.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'LoteCobVGerado' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<LoteCobVGerado> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(LoteCobVGerado.class));

       return (TypeAdapter<T>) new TypeAdapter<LoteCobVGerado>() {
           @Override
           public void write(JsonWriter out, LoteCobVGerado value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public LoteCobVGerado read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of LoteCobVGerado given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of LoteCobVGerado
   * @throws IOException if the JSON string is invalid with respect to LoteCobVGerado
   */
  public static LoteCobVGerado fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, LoteCobVGerado.class);
  }

  /**
   * Convert an instance of LoteCobVGerado to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

