/*
 * Poupança
 * <pre>[ Base URL: https://api.sicoob.com.br/poupanca/v3 ]</pre> # Poupança API Sistema que faz a gestão das contas de poupança através de transãções como Aplicação, Resgate, Saldo, Extrato e Controle de Rentabilidade. 
 *
 * The version of the OpenAPI document: 3.0.4.0
 * Contact: marco.martins@sicoob.com.br
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package br.com.sicoob.poupanca.model;

import java.util.Objects;
import br.com.sicoob.poupanca.model.ResumoExtrato;
import br.com.sicoob.poupanca.model.Transacao;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import br.com.sicoob.poupanca.JSON;

/**
 * Propriedade que contem o resultado da requisição
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2026-05-04T12:32:58.564497-03:00[America/Sao_Paulo]", comments = "Generator version: 7.22.0")
public class ExtratoResultado {
  public static final String SERIALIZED_NAME_RESUMO = "resumo";
  @SerializedName(SERIALIZED_NAME_RESUMO)
  @javax.annotation.Nonnull
  private ResumoExtrato resumo;

  public static final String SERIALIZED_NAME_TRANSACOES = "transacoes";
  @SerializedName(SERIALIZED_NAME_TRANSACOES)
  @javax.annotation.Nonnull
  private List<Transacao> transacoes = new ArrayList<>();

  public ExtratoResultado() {
  }

  public ExtratoResultado resumo(@javax.annotation.Nonnull ResumoExtrato resumo) {
    this.resumo = resumo;
    return this;
  }

  /**
   * Get resumo
   * @return resumo
   */
  @javax.annotation.Nonnull
  public ResumoExtrato getResumo() {
    return resumo;
  }

  public void setResumo(@javax.annotation.Nonnull ResumoExtrato resumo) {
    this.resumo = resumo;
  }


  public ExtratoResultado transacoes(@javax.annotation.Nonnull List<Transacao> transacoes) {
    this.transacoes = transacoes;
    return this;
  }

  public ExtratoResultado addTransacoesItem(Transacao transacoesItem) {
    if (this.transacoes == null) {
      this.transacoes = new ArrayList<>();
    }
    this.transacoes.add(transacoesItem);
    return this;
  }

  /**
   * Lista com as transações efetivadas de uma conta poupança em determinado mês e ano
   * @return transacoes
   */
  @javax.annotation.Nonnull
  public List<Transacao> getTransacoes() {
    return transacoes;
  }

  public void setTransacoes(@javax.annotation.Nonnull List<Transacao> transacoes) {
    this.transacoes = transacoes;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    ExtratoResultado extratoResultado = (ExtratoResultado) o;
    return Objects.equals(this.resumo, extratoResultado.resumo) &&
        Objects.equals(this.transacoes, extratoResultado.transacoes);
  }

  @Override
  public int hashCode() {
    return Objects.hash(resumo, transacoes);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class ExtratoResultado {\n");
    sb.append("    resumo: ").append(toIndentedString(resumo)).append("\n");
    sb.append("    transacoes: ").append(toIndentedString(transacoes)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    return o == null ? "null" : o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("resumo", "transacoes"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(Arrays.asList("resumo", "transacoes"));
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to ExtratoResultado
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!ExtratoResultado.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in ExtratoResultado is not found in the empty JSON string", ExtratoResultado.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!ExtratoResultado.openapiFields.contains(entry.getKey())) {
          // // throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` in the JSON string is not defined in the `ExtratoResultado` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
          // Ignoring unknown fields to prevent deserialization errors
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : ExtratoResultado.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // validate the required field `resumo`
      ResumoExtrato.validateJsonElement(jsonObj.get("resumo"));
      if (jsonObj.get("transacoes") != null) {
        if (!jsonObj.get("transacoes").isJsonArray()) {
          // throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `transacoes` to be an array in the JSON string but got `%s`", jsonObj.get("transacoes").toString()));
        }
        JsonArray jsonArraytransacoes = jsonObj.getAsJsonArray("transacoes");
        // validate the required field `transacoes` (array)
        for (int i = 0; i < jsonArraytransacoes.size(); i++) {
          Transacao.validateJsonElement(jsonArraytransacoes.get(i));
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!ExtratoResultado.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'ExtratoResultado' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<ExtratoResultado> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(ExtratoResultado.class));

       return (TypeAdapter<T>) new TypeAdapter<ExtratoResultado>() {
           @Override
           public void write(JsonWriter out, ExtratoResultado value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public ExtratoResultado read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of ExtratoResultado given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of ExtratoResultado
   * @throws IOException if the JSON string is invalid with respect to ExtratoResultado
   */
  public static ExtratoResultado fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, ExtratoResultado.class);
  }

  /**
   * Convert an instance of ExtratoResultado to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

